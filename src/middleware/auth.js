export default function (context) {
    // if (typeof localStorage.JWT == 'undefined' || localStorage.JWT.length <= 9) {
    //     // 未登录
    //     if (context.route.name != 'login-login') {
    //         context.app.router.replace('/login/login');
    //     }
    // }
}